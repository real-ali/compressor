import 'package:compressor/compressor.dart';
import 'package:test/test.dart';

void main() {
  test('Encode Video', () {
    expect(CompressorVideo(), 42);
  });

  test('Decode Video', () {
    expect(CompressorVideo(), 42);
  });
}
