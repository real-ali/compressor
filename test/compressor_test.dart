import 'package:compressor/compressor.dart';
import 'package:test/test.dart';

void main() {
  test('Encode Video', () {
    expect(Compressor, 42);
  });

  test('Decode Video', () {
    expect(Compressor(), 42);
  });
}
